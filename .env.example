# Optional: for on-chain RPC mode (more accurate but slower)
POLYGON_RPC_URL=https://polygon-rpc.com

# Optional: for high-performance backfills via Envio HyperSync
# Get a free token at https://envio.dev/app/api-tokens
HYPERSYNC_API_TOKEN=
